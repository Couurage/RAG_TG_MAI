# RAG-TG-MAI

Современные нейросети впечатляют возможностями генерации текста, но у них есть фундаментальный изъян:  
модель **не знает того, чего не видела на обучении** и легко придумывает факты.  
Для работы с конкретными данными — лекциями, методичками, внутренними документами — обычная LLM непригодна.  

Здесь появляется **RAG (Retrieval-Augmented Generation)** — подход, который соединяет сильные стороны двух миров:  
1) хранение знаний в специализированной базе,  
2) генерацию ответа нейросетью на основе найденных фактов.

RAG позволяет модели не "угадывать", а опираться на реальные данные.  
Ты задаёшь вопрос → система ищет нужные фрагменты в векторной базе → формирует контекст → LLM создаёт точный, аргументированный ответ.

**RAG_TG_MAI** — это проект по построению такой системы.  
В нём происходит полный цикл:  
- загрузка документов (конспектов, лекций, методичек),  
- нарезка текста на чанки,  
- генерация эмбеддингов через Gemma,  
- сохранение их в Milvus,  
- поиск по запросу,  
- (позже) формирование ответа через LLM и Telegram-бот.

Итоговая цель — создать ассистента, который "читает" твои учебные материалы и отвечает на вопросы как умный репетитор: аккуратно, корректно и с привязкой к контенту, который ему реально дали.

---

# 📚 RAG_TG_MAI

RAG_TG_MAI — это фундаментальная база для RAG-ассистента: система, которая превращает текстовые документы в векторное пространство и позволяет выполнять поиск с последующей генерацией ответа на основе найденных фрагментов.  
Проект уже включает устойчивую архитектуру низкого уровня — модель, токенизацию, эмбеддинги, Milvus, тестовый пайплайн — и готов к расширению в Telegram-бота.

---

## 🔎 Что уже реализовано

- Токенизация и чанкинг текста (`backend/gemma_services/tokenizer.py`)
- Генерация эмбеддингов Gemma-моделью (`backend/gemma_services/embedder.py`)
- Ленивая загрузка модели (`backend/models/gemma.py`)
- Обёртка над Milvus (создание коллекции, вставка, поиск)
- Конфигурационный модуль (`config.py`)
- Docker-окружение для Milvus Standalone (`docker-compose.yml`)
- Тестовый сценарий вставки/поиска (`main.py`)

Планируется:
- интеграция с LLM (полный RAG-ответ),
- реализация Telegram-бота,
- загрузка учебных материалов большими партиями.

---

## 🧱 Технологический стек

**Python 3.12**, **PyTorch**, **HuggingFace Transformers**,  
**Milvus**, **pymilvus**, **Docker**,  
(будущее: **aiogram**, любая LLM-модель)

---

## 🗂 Структура проекта

```text
RAG_TG_MAI/
├── backend/
│   ├── gemma_services/
│   │   ├── tokenizer.py     # Разбиение текста
│   │   └── embedder.py      # Эмбеддинги
│   │
│   ├── models/
│   │   └── gemma.py         # Ленивый загрузчик полной модели Gemma
│   │
│   ├── database/
│   │   └── milvus.py        # Работа с коллекцией Milvus
│
├── config.py
├── docker-compose.yml
├── main.py
├── requirements.txt
└── README.md
```

---

## 🚀 Быстрый старт

### 1. Установка

```bash
git clone https://github.com/Couurage/RAG_TG_MAI.git
cd RAG_TG_MAI

python -m venv .venv
source .venv/bin/activate      # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Запуск Milvus

```bash
docker-compose up -d
```

### 3. Настройки

В файле `config.py` задаются хост Milvus, порт, имя коллекции и параметры модели.

### 4. Проверка пайплайна

```bash
python main.py
```

---

## 🧠 Как работает система (коротко)

1. Документ разбивается на смысловые чанки.  
2. Каждый чанк преобразуется в эмбеддинг (вектор).  
3. Вектора загружаются в Milvus — векторную базу данных.  
4. Запрос пользователя тоже превращается в эмбеддинг.  
5. Milvus ищет наиболее близкие фрагменты.  
6. (позже) LLM формирует ответ, опираясь на найденные данные.

Таким образом модель отвечает ровно на основе тех материалов, которые ей дали, без “галлюцинаций”.

---

## 🧪 Пример использования

```python
from pymilvus import connections, utility
from backend.database.milvus import Milvus
from backend.gemma_services.embedder import Embedder
from backend.gemma_services.tokenizer import Tokenizer

milvus = Milvus()
tokenizer = Tokenizer()
embedder = Embedder()

connections.connect(
    "default",
    host=milvus.host,
    port=milvus.port,
)

print("Коллекции:", utility.list_collections(using=milvus.alias))
print("Текущая коллекция:", milvus.collection.name)

coll = milvus.collection
coll.load()

text = "Тестовый текст про Milvus, чтобы проверить вставку."
tokens = tokenizer.chunk_markdown(text)
embedding = embedder.embed(tokens)

coll.add_chunks(
    chunk_id=0,
    section="intro",
    source_path="test.md",
    doc_id=1,
    embedding=embedding,
)
```

---

## 🗺 Roadmap

- улучшение схемы коллекции Milvus,  
- подключение LLM и создание полноценного RAG-ответа,  
- Telegram-бот ассистент,  
- автоматическая загрузка лекций и PDF,  
- функции высокого уровня для удобного вставления и поиска.

---

## 👤 Автор

**Alexander Malakhov (Couurage)**  
GitHub: https://github.com/Couurage

---

Если проект нравится — ⭐ всегда поможет ему расти.
